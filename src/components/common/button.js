import React from 'react'

const Button = ({text}) => {
  return (
    <div className=' text-center px-4 py-2 ml-4 bg-gradient-primary rounded-2xl shadow-5xl'>{text}</div>
  )
}

export default Button